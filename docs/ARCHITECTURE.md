# 架构总览

这个仓库当前已经从模板收敛成一个本地 `computer-use` 项目。主线仍是 Swift 实现的 macOS automation MCP server，同时新增了实验性的 Windows 和 Linux runtime，用独立 Go 二进制暴露同一组 9 个 Computer Use tools。

## 当前目录结构

- `apps/OpenComputerUse`
  主入口，负责 `mcp`、`event-stream`、`doctor`、`list-apps`、`snapshot`、`call`、`turn-ended` 等 CLI 命令，以及 `-h` / `--help` / `-v` / `--version` 这类全局参数；不带参数启动时会先检查权限，只有缺失时才进入无 Dock 图标的 app 模式权限引导窗口，`doctor` 也只会在检测到缺失权限时拉起这套 onboarding UI。
- `apps/OpenComputerUseFixture`
  本地 GUI fixture app，用来承载低风险、可预测的点击/输入/滚动/拖拽验证路径。
- `apps/OpenComputerUseSmokeSuite`
  端到端 smoke runner，会拉起 fixture 和 MCP server，并通过 JSON-RPC 真实调用 9 个 tools；同时也支持单独的 visual cursor idle smoke，用跨进程 observation file 断言等待下一次 move 时是 anchored tip + tiny rotate wobble，而不是横向漂移。
- `apps/OpenComputerUseWindows`
  实验性 Windows runtime。它不依赖 Swift 或 `.app` bundle，Go CLI/MCP 入口会嵌入 PowerShell UI Automation bridge，构建产物是 `open-computer-use.exe`，并随已有 npm 包的 `dist/windows/<arch>/` bundled artifacts 分发。
- `apps/OpenComputerUseLinux`
  实验性 Linux runtime。它不依赖 Swift 或 `.app` bundle，Go CLI/MCP 入口会嵌入 Python AT-SPI bridge，构建产物是 `open-computer-use`，并随已有 npm 包的 `dist/linux/<arch>/` bundled artifacts 分发。
- `packages/OpenComputerUseKit`
  核心库，包含：
  - MCP stdio transport 与 tool registry
  - app discovery
  - Accessibility / 窗口 snapshot
  - 键鼠输入模拟
  - software cursor overlay
  - fixture test bridge
- `experiments/CursorMotion`
  独立的 Swift cursor motion lab，用于试验 `Bezier + arc + spring` 参数模型、调参 UI 和独立渲染，不直接耦合主 MCP runtime。
- `experiments/StandaloneCursor`
  新的独立 Swift cursor viewer，直接复用 `scripts/cursor-motion-re/official_cursor_motion.py` 里收敛出来的候选路径、score 与 raw spring timeline，用来观察更贴近 binary lift 的表现。
- `scripts/`
  仓库级自动化命令，包括 smoke test、`.app` 打包入口、Windows `.exe` / Linux binary 构建入口、npm 分发脚本，以及 `scripts/computer-use-cli/` 这个用于探测官方 bundled `computer-use` 的 Go helper。
- `skills/`
  面向 agent runtime 的可安装 skill。`skills/open-computer-use/SKILL.md` 是通用 Computer Use 入口，安装、MCP/CLI 使用、排障和 Record & Replay 细节拆到相邻 `references/` 文件里按需加载；`skills/open-computer-use-record-and-replay/SKILL.md` 是独立 Record & Replay thin skill，只规定 macOS 录制、停止、读取 session 产物和生成可复用 skill 的流程，便于后续迁到独立 repo。`scripts/scaffold-record-and-replay-skill-repo.py` 可把这个 thin skill 导出成 standalone repo 骨架，npm 包也暴露同等安装态命令 `open-computer-use scaffold-record-and-replay-skill-repo --output-dir <dir>`；生成物只依赖已安装的 `open-computer-use` runtime，不复制本仓库源码。骨架内自带 `scripts/package-skill.sh`、`scripts/verify-manifest.py`、`scripts/verify-source-baseline-summary.py`、`scripts/verify-readme-handoff.py`、`scripts/verify-runtime.py`、`scripts/verify-skill-workflow.py`、`scripts/wait-notify-contract-smoke.py`、`scripts/recording-to-skill-smoke.py`、组合门禁 `scripts/check.sh`、可选真实录制闭环检查 `scripts/recording-lifecycle-smoke.py` 和 pin 到 commit SHA 的 `.github/workflows/ci.yml`，并在 `record-and-replay-skill-repo.json` 里把默认 `checks`、OCU `extensionLayer` 与可选 `optionalChecks` 分开声明。默认自检不启动录制，会校验 skill frontmatter、打包 skill、用 `scripts/verify-manifest.py` 校验 `record-and-replay-skill-repo.json` 里的官方证据、source repo audit、`manifestContract`、OCU 扩展边界和 recording-to-skill 门禁没有漂移，用 `scripts/verify-source-baseline-summary.py` 审计随 scaffold 复制的 `evidence/source-baseline-summary.json`，确认源仓 `dist/record-and-replay-baseline-summary.json` 的脱敏投影仍显示 `usableBaseline=true`、`standaloneRepoBaselineReady=true`，official golden 状态要么保持当前 required gap 明确、要么在未来 official golden/equivalence ready 时完成态自洽，`preflightPipelines.checkedOfficialCapturePacketSetContractManifest=true`、`preflightPipelines.checkedOfficialCapturePacketPostCaptureWorkflow=true`、`preflightPipelines.checkedOfficialCapturePacketSetPostCaptureWorkflow=true`、`preflightPipelines.checkedOfficialCapturePacketStrictAuditHandoff=true` 和 `preflightPipelines.checkedOfficialCapturePacketStrictExpectedFailureAuditHandoff=true` 被保留，并且 `checkedSourceBaselineSummaryEvidence=true` / `checkedSourceBaselineSummaryOfficialGoldenState=true` / `checkedSourceBaselineSummaryCapturePacketSetContractManifest=true` / `checkedSourceBaselineSummaryCapturePacketPostCaptureWorkflow=true` / `checkedSourceBaselineSummaryCapturePacketSetPostCaptureWorkflow=true` / `checkedSourceBaselineSummaryCapturePacketStrictAuditHandoff=true` / `checkedSourceBaselineSummaryCapturePacketStrictExpectedFailureAuditHandoff=true` 进入 source / npm staged evidence，用 `scripts/verify-readme-handoff.py` 校验 README 里的官方证据、baseline / strict audit handoff、official golden 缺口、recording-to-skill 命令和 wait/notify 边界没有漂移，握手验证 `open-computer-use event-stream mcp` 的官方三件套 surface 与 no-active status/stop response shape，验证 thin skill 保留官方 handoff 语义（start 后结束 turn、不轮询、用户完成后 stop、取消不再 stop）、OCU wait/notify 扩展边界（listener 不进入 MCP surface、notify 通过 stdin/env 回调），并通过等待不存在的 session 验证 `waitSessionMatched=false` 时 callback skipped 且不创建 session 文件，通过合成 completed session 验证 notify callback 能收到 `OPEN_COMPUTER_USE_EVENT_STREAM_SUPPRESSED_EVENTS_PATH`，且非零退出或超时都会让 CLI 非零，并分别返回 `reason=nonZeroExit` / `reason=timeout`，再用临时合成 recording 验证安装态 `validate --require-skill-draft` / events-only gate / `scaffold-skill` 可生成 skill 草稿，同时用无动作 recording 验证失败的 `scaffold-skill` 会返回非零退出码；需要本地确认真实 session 文件链路时，可显式运行 lifecycle smoke 验证重复 start 返回同一 active session、stop 幂等、停止后的 status 仍指向同一个 completed session，并用安装态 runtime validate 校验 recording 产物。`scripts/package-skill.sh` 会扫描 `skills/*/SKILL.md`，校验 frontmatter 名称和描述，并为每个 skill 打包 `.zip` / `.skill` 制品。
- `docs/`
  逆向分析、执行计划、history 和项目约束。

## 运行分层

### 1. App Mode 层

- `OpenComputerUse` 默认 app 模式会拉起 `PermissionOnboardingApp`。
- app bundle 以 `LSUIElement` agent-style 形态运行，默认不在 Dock 暴露常驻图标，但仍可按需显示权限窗口。
- 当用户从终端执行 macOS 版 `open-computer-use mcp`、`doctor`、`call`、`snapshot` 或 `list-apps` 时，CLI 会先通过 LaunchServices 启动同一个 `.app` bundle 的隐藏 app agent，并通过用户临时目录下的 Unix domain socket 转发请求；真正调用 Accessibility、ScreenCaptureKit 和动作 tools 的进程始终是 `Open Computer Use.app`，不是 iTerm / Terminal / Node launcher。
- 主窗口负责渲染 `Accessibility` / `Screen & System Audio Recording` 两类权限卡片、`Allow` / `Done` 状态和 relaunch 后的状态收敛；当两项权限都已完成时会自动关闭，不再要求用户手动退出。
- 辅助 drag panel 会跳转到对应的 `System Settings` 页面；点击 `Allow` 后，panel 会从主窗口里的按钮位置做一段 spring + curved frame 的入场，再落到 `System Settings` 内容区下沿。panel 默认保持在窗口右侧内容区下方居中并固定贴近窗口底边，不再依赖实时扫描权限页内部 `+ / -` 控件行；窗口层级上会显式排在当前 `System Settings` 窗口之上，避免被权限列表内容盖住，同时尽量减少对系统设置自身滚动区域的干扰。panel 内也补了显式返回按钮，允许用户中断当前 guidance、回到 onboarding 主窗口重新选择权限步骤。
- 权限状态会合并 TCC 持久授权记录与当前 app 进程的 runtime preflight：TCC 中任一匹配 client 已授权即可视为 granted，避免 CLI 子进程与 GUI app 对授权状态看到不一致的结果；如果当前 `.app` 进程已经通过 `AXIsProcessTrusted()` / `CGPreflightScreenCaptureAccess()`，也会立即视为 granted，避免 stale 或不匹配的 TCC path 记录让 onboarding 浮层继续停留。正式 release 仍以 CI 打出来的 `Open Computer Use.app` 为准，而本地 debug/dev 打包现在显式命名为 `Open Computer Use (Dev).app`，并在 dev bundle 运行时优先认当前 dev 副本，避免系统设置里出现两个完全同名的条目。

### 2. MCP 层

- 面向 MCP host 的外部 transport 仍是 `stdio`；macOS 终端 CLI 到 `.app` app agent 之间额外有一层本地 Unix domain socket 代理，用来保证真实 automation 运行在 app bundle 权限身份下。
- `open-computer-use event-stream mcp` 启动 Record & Replay 兼容的独立 MCP surface，server name 为 `Record & Replay`，当前暴露 `event_stream_start`、`event_stream_status`、`event_stream_stop` 三个无参数 tools；它复用同一套 stdio framing 和 app-agent 代理，但使用独立的 event-stream dispatcher。app-agent 内的 event-stream MCP server 按需创建，避免在 CLI `event-stream start` 的环境覆盖生效前提前初始化共享 recorder。start approval policy 设为 `mcp` / `elicitation` 时，server 只在 host 的 `initialize` 明确声明 `capabilities.elicitation` 后发送内部 `elicitation/create` 请求，并把 accept / decline / cancel 映射回 recorder approval；tools/list 和 tool input schema 不因此增加任何参数。
- Codex 安装入口现在分成两个幂等脚本：`open-computer-use install-codex-mcp` 写入普通 9-tool Computer Use server，`open-computer-use install-codex-record-and-replay-mcp` 写入独立 `record_and_replay` server，启动参数为 `["event-stream","mcp"]`。这避免上层 skill/plugin 继续手写 TOML，也避免把 Record & Replay 扩展混入普通 MCP server。
- 当 `OPEN_COMPUTER_USE_VISUAL_CURSOR` 未被显式关闭时，`mcp` 命令会切到一个最小 AppKit runtime：主线程保留 event loop 承载 overlay UI，stdio server 仍在后台线程串行读取与响应。
- 请求 framing 采用一行一个 JSON-RPC message。
- 当前支持的 method：
  - `initialize`
  - `notifications/initialized`
  - `notifications/turn-ended`
  - `ping`
  - `tools/list`
  - `tools/call`
- `notifications/turn-ended` 是开源版显式的 turn boundary hook；收到后会清理当前进程里的 visual cursor overlay。CLI `open-computer-use turn-ended [payload]` 也会通过 macOS distributed notification 通知正在运行的 AppKit MCP 进程执行同一类清理，用于接 Codex legacy notify 的 after-agent payload。

### 3. Tool Service 层

- `ComputerUseService` 负责把 Computer Use tool 请求映射到本地能力，`ComputerUseToolDispatcher` 则把 9 个 tool 的参数解析与 service 方法分发收敛成 MCP server 和 `open-computer-use call` 共用的一层。
- `EventStreamService` 负责 Record & Replay baseline 录制状态。它在长驻 app-agent / MCP server 进程内维护 active session，写入 `events.jsonl`、OCU 完整状态 `metadata.json`、官方风格最小 handoff `session.json`、`suppressed.jsonl`、`latest-session.json` 和录制中的 `active-session.json`，并记录 `session.started`、`window.changed`、`AX.focusedWindowChanged`、`mouse.click`、`mouse.context_menu`、`mouse.drag`、`keyboard.text_input`、`keyboard.submit`、`keyboard.shortcut`、`terminal.value_changed`、`selection.changed`、`debug.error`、`session.ended` 这类基础事件；主事件和 suppressed 事件都会双写官方观察到的 `kind` 字段与 OCU 既有兼容字段 `type`，源码 validator / summarizer / fixture import / readiness / compare 链路也接受 `kind`-only 官方样本；官方兼容 MCP start/status/stop response 会暴露 `sessionID`、`sessionDirectoryPath`，并让 `metadataPath` 指向 `session.json`，贴近 Codex-hosted completed session handoff；Swift/Python strict validator 在同目录存在 `metadata.json` 时仍以完整 OCU metadata 校验计数、路径和状态，同时接受 `session.json` 与 `metadata.json` 语义兼容但不字节相等。鼠标事件现在按 down / dragged / up 维护 pending pointer，只有 mouse up 后才决定写 `mouse.click` / `mouse.context_menu` 还是 `mouse.drag`，避免把拖拽误记成点击。`mouse.click` / `mouse.context_menu` 会记录 y-down screen-state `location`、原始 AppKit `appKitLocation`，并尽量通过 `AXUIElementCopyElementAtPosition` 补 `targetAccessibilityElement`；`mouse.drag` 会记录起止 y-down `startLocation` / `endLocation`、起止 AppKit location、距离、时长和同样的目标 AX 摘要。键盘事件会尽量补当前 `focusedAccessibilityElement`，帮助后续 skill 识别用户输入、提交和快捷键目标；如果 focused element 暴露 protected/secure/password 信号，则只写 `secureInput=true` 和文本长度，且 raw key event 不写明文 `characters`；如果 focused element 属于明显终端上下文，则键盘 raw characters 和 focused element value 也会脱敏，并在 AX value 变化时写轻量 `terminal.value_changed`，只保留 value hash / length。输入事件后如果当前 focused AX element 暴露非空 `selectedText` 且 selection signature 变化，会写轻量 `selection.changed` 事件；如果此前有选择而当前选择为空，会写 `selectionCleared=true` 的清空事件；secure input、terminal value、selection 字段与清空选择语义仍待 official golden recording 校准。官方字符串出现过的 `experimentalRawEvents` 现在作为 OCU baseline 默认开启，可用 `OPEN_COMPUTER_USE_EVENT_STREAM_RAW_EVENTS=0` 关闭；点击、拖拽、键盘事件会附带底层 NSEvent 摘要，scroll wheel 在未确认官方独立 scroll event 名称前只写 `kind=experimentalRawEvents` / `type=experimentalRawEvents` 的 raw-only 事件，不臆造 `mouse.scroll`。`window.changed` 会在录制开始和输入事件前 app/window context 变化时写入，并继续补 `AX.focusedWindowChanged` 作为 AX 上下文；`AX.focusedWindowChanged` 会尽量附带 `accessibilityInspectorPayload`，首个 payload 是 full tree，并双写官方观察到的 `fullTree` 与 OCU 既有 `treeLines`，同一 app/window 的后续 payload 会用轻量 `~` / `+` / `-` compact diff，diff baseline 已改为 LCS 风格行 diff 并归一化 AX tree leading element index，避免插入元素后把后续稳定行误判为变更；diff payload 会保留 previous diff 并附加 cumulative diff baseline。字段、初始帧语义、diff 算法和事件顺序仍待 official golden recording 校准。AX snapshot 失败、cumulative diff 过大、截图写入失败和 Input Monitoring monitor 全部安装失败等上下文采集降级会写低敏 `debug.error` 主事件，并继续把更完整细节写入 `suppressed.jsonl`；其中 input monitor 不可用会写 `subsystem=inputMonitoring` / `reason=inputMonitorsUnavailable`，避免独立集成方拿到无输入事件的空录制却没有诊断信号。截图上下文由 `OPEN_COMPUTER_USE_EVENT_STREAM_SCREENSHOTS=auto|always|never` 控制，默认 `auto`，只在 payload 标记 `screenshotNeededForContext` 且 snapshot 有截图数据时写入 session 的 `screenshots/` 并返回 `screenshotPath`。录制默认最多持续 30 分钟，超时会自动写入 `session.ended` 并以 `recording_time_limit_reached` 结束；该 endReason 是 OCU baseline，仍待 official golden recording 校准。测试和本地调试可用 `OPEN_COMPUTER_USE_EVENT_STREAM_MAX_DURATION_SECONDS` 覆盖时长。`event-stream start/status/stop/cancel/wait --json` 会通过 app-agent 访问同一份进程内状态；app-agent 对 `status/stop/cancel/wait` 不持有全局环境覆盖锁，避免上层 listener 正在 `wait` 时阻塞另一个 `stop` / `cancel`；`wait --json` 是 OCU 扩展层 listener，会在返回体增加 `waitTimedOut` 和 `waitSessionMatched` 标记，方便独立 plugin/skill 区分结束唤醒、超时返回和 session id 不匹配。`wait --session-id` 会优先读取目标 session 目录中的完成态 metadata，即使 latest session 已被后续录制覆盖也能返回指定历史 session；未知或不匹配的 session id 会快速返回 `waitTimedOut=true` / `waitSessionMatched=false` 并跳过 notify callback。未通过 app-agent 的短生命周期 CLI 只适合调试。如果新的 recorder 进程发现 `latest-session.json` 仍是 active 状态但进程内没有 active session，会把它持久化改写为 `state=stopped`、`endReason=recording_process_unavailable` 并清理 stale `active-session.json`，避免独立集成方一直看到幽灵录制。start 前已接 OCU baseline approval gate：已有 active session 时直接返回，新的 session 只有 approved 后才创建；denied / cancelled 使用官方已观察错误文案且不写 session 文件，`OPEN_COMPUTER_USE_EVENT_STREAM_START_APPROVAL=auto|interactive|approve|deny|cancel|mcp|elicitation` 可控制策略，自动化验证显式使用 `approve`，MCP elicitation smoke 显式使用 `mcp`。交互式录制默认先展示 OCU 自有开始确认弹窗，开始后展示 Record & Replay 控制条，支持 Done、Discard、计时和拖动；自动化验证可用 `OPEN_COMPUTER_USE_EVENT_STREAM_CONTROLS=0` 关闭控制条。
- EventStream 输入监听在生产路径优先安装 listen-only `CGEvent` tap，捕获 session 级鼠标、键盘和 scroll 输入；如果 event tap 不可用，再回退到原 `NSEvent.addGlobalMonitorForEvents` 路径。`make event-stream-action-smoke` 是 opt-in 真实输入链路验证：默认 `mixed-action-stop` 让 app-agent recorder 录制外部进程发出的低影响 CGEvent click，并继续发送 best-effort scroll wheel；release baseline 必需证据是 `mouse.click`、AX context、MCP transcript 和 skill handoff，捕获到的 scroll raw-only `experimentalRawEvents` 仍会进入 summary/scaffold，但不作为硬门槛。设置 `OPEN_COMPUTER_USE_EVENT_STREAM_SMOKE_ACTION_SCENARIO=simple-action-stop|drag-stop` 时，同一 smoke 会分别采样 click 或 drag，用于生成对应 OCU candidate。完整 baseline audit 会同时消费默认 mixed action smoke 和 simple / drag candidate smoke，并在 `evidence.realInputActionSmoke` 中要求 `checkedSimpleActionStopCandidate` / `checkedDragStopCandidate` 为 true；这只是 OCU candidate 证据，仍不能替代 official successful recording golden。`keyboard-input-stop` 当前通过已有 recording / smoke JSON 导入和 readiness 校验覆盖，不走 synthetic keyboard `--run-action-smoke`，避免 macOS 过滤合成键盘事件造成真实输入 smoke 不稳定。summary/scaffold 会把捕获到的 `reason=scrollWheel` raw-only event 渲染成 Scroll replay step，同时确认草稿和 `recording-summary.json` 不泄漏本机绝对路径且 summary 仍可创建 skill 草稿。
- OCU 的 `event-stream wait --notify-command '<json-argv>'` 是独立集成回调扩展，不属于官方兼容 MCP surface；它只在 `waitTimedOut=false` 时同步执行本地 callback，stdin 写入最终 status JSON，并通过 `OPEN_COMPUTER_USE_EVENT_STREAM_STATUS_JSON`、`OPEN_COMPUTER_USE_EVENT_STREAM_SESSION_ID`、`OPEN_COMPUTER_USE_EVENT_STREAM_STATE`、`OPEN_COMPUTER_USE_EVENT_STREAM_END_REASON`、`OPEN_COMPUTER_USE_EVENT_STREAM_METADATA_PATH`、`OPEN_COMPUTER_USE_EVENT_STREAM_SESSION_PATH`、`OPEN_COMPUTER_USE_EVENT_STREAM_EVENTS_PATH` 和 `OPEN_COMPUTER_USE_EVENT_STREAM_SUPPRESSED_EVENTS_PATH` 环境变量传递上下文；callback 默认 10 秒超时，可用 `OPEN_COMPUTER_USE_EVENT_STREAM_NOTIFY_TIMEOUT_SECONDS` 覆盖，非零退出或超时会让 CLI 返回 error 并写入 `notification` 结果。若 `waitSessionMatched=false`，返回体同时会有 `waitTimedOut=true`，callback 会被跳过。status / metadata JSON 里的 `sessionPath` 是 OCU 便利字段，指向官方风格 `session.json` handoff 文件；官方兼容消费仍以 `metadataPath` / `eventsPath` 为主。
- `list_apps` 通过 Spotlight metadata query 拉取标准 application 目录里的 app bundle，并读取 `kMDItemUseCount` / `kMDItemLastUsedDate_Ranking` 这类系统元数据；再与 `NSWorkspace` 的运行态 app 合并，输出“当前运行中 + 近 14 天用过”的视图。
- `get_app_state` 优先走真实 AX / 窗口截图；真实 app 必须同时有未最小化的 `AXWindow` 和可匹配的 on-screen `CGWindow`。如果目标 app 只是隐藏或暂时没有 on-screen window，会先 best-effort unhide / activate / `open -b` / `AXRaise` 并短暂重试，以贴近官方 `computer-use` 会把 Lark / Electron 窗口拉回再采集的行为；恢复后仍无法匹配时返回官方风格的 `Apple event error -10005: cgWindowNotFound`，不再把 application 根节点或无截图窗口伪装成可操作状态。当目标是仓库内 fixture app 时，回退到 fixture 导出的合成状态。真实 AX tree 对 Electron/WebView 这类深层 UI 会放宽遍历深度，同时压缩空 `AXGroup` / `AXUnknown` wrapper、过滤 `AXScrollToVisible` 噪音和空字符串属性，避免 action-critical 的输入框被无语义容器挤出节点预算；对原生 open panel / Finder column view 这类把内容放在 `AXContents` / `AXVisibleChildren` 里的控件，也会把可见文件项纳入元素树。
- MCP `tools/list` 的 description / input schema 当前按官方 `computer-use` 的 9 个 tools 文案和参数面收敛，尽量减少 host 侧提示词和 tool surface 偏差。
- `open-computer-use call <tool> --args '{...}'` 会直接输出 MCP-style JSON result；`open-computer-use call --calls '[...]'` / `--calls-file <path>` 会在同一进程里顺序执行 JSON 数组里的 tool calls，并复用同一个 `ComputerUseService` 内存态，因此 `get_app_state` 之后的 action tool 可以继续使用同一轮 snapshot 的 `element_index`。序列执行默认会在成功的相邻操作之间 sleep 1 秒，也可以用 `--sleep <seconds>` 覆盖；遇到 `isError=true` 的 tool result 后停止。
- 对真实 app 的 `get_app_state` / action tool 入口，当前只保留一层密码管理器 bundle denylist：bundle-id 直传时直接返回 safety denial；名称匹配时默认不解析到这些 app。终端、Chrome / Atlas 和系统组件不再属于内置阻止目标。
- 普通 app 的 element frame 当前按“窗口左上角为原点”的 window-relative 坐标输出，便于后续把 `element_index` 和截图坐标统一到同一套参考系。
- `click` / `set_value` 在执行真实动作前后，会额外驱动一层透明 `SoftwareCursorOverlay` window：两者的移动阶段现在共用一条 heading-driven 的官方风格 motion 内核，显式把“当前 cursor 朝向”和“最终 resting pose”一起喂给选路器，优先生成需要时先掉头、再沿车头方向推进的 C 形/单侧大弧轨迹；首次显示时按官方 binary 的 fresh state 从 AppKit 全局 `(0,0)` window origin 生成起点，后续动作继续复用上一帧 visible tip。真正显示出来的 cursor 不再直接等于 path sample，而是经过一层独立的 visual dynamics 状态，把 visible tip、velocity、angle 和 fog/offset 持续推进。`click` 结尾会衔接 click pulse 和更明显但仍然很小的 rotate wobble，`set_value` 则只做 settle / idle，不给 pulse；两者收尾后会在目标点继续保持 idle 状态，等待下一次动作时 tip 保持 anchored、只保留可感知的小角度摆动；只有连续 30 秒没有新动作时才做 cleanup，这样连续 tool call 不会反复从 fresh `(0,0)` 起步；如果宿主在任务 / turn 结束时发出 `turn-ended`，cursor 会立即消失并清掉本轮位置状态。
- overlay 的 visual style 不再自己从官方 app bundle 裁 `SoftwareCursor` 小图；主 MCP runtime 现在和 `CursorMotion` 一样优先渲染仓库里沉淀的 `official-software-cursor-window-252.png` baseline，只有资源缺失时才退回 `OpenComputerUseKit` 内部的程序化 pointer/fog fallback。命中点 anchor 仍固定在 `126x126` 画布里的同一组 tip-offset 上；glyph 自身的 neutral heading 继续沿用 `CursorMotion` / 官方 baseline 的 `-3π/4`。主 runtime overlay window 按 AppKit 全局坐标移动，因此在把 AX / `CGWindowList` 产出的 y-down screen-space 点击目标喂给 overlay 之前，会先转换成对应屏幕的 AppKit 全局坐标；路径选路用屏幕上实际可见的 AppKit forward heading，进入 visual dynamics / render state 前则把 velocity 的 y 轴翻回 CursorMotion 的 y-down screen state，再交给 AppKit 绘制层做角度和 `dy` 翻转。程序化 fallback 保留 neutral artwork correction，把它的天然轮廓轴对齐到 `CursorMotion` / 官方 baseline 的 `-3π/4` forward 方向，但不让实验线依赖 runtime 代码。
- overlay 的层级不再固定 `.floating`；现在会跟随 snapshot 命中的目标 window id / layer，把自己排到该目标 window 之上，而不是粗暴压到所有前台 app 最上层。
- overlay 的曲线路径不再只按固定 Bezier 模板生成；当前主线采用 reverse-engineering 约束下的 heading-driven candidate 族，候选只保留 `direct` / `turn` / `brake` / `orbit` 这些能稳定产出单侧主弧的 family，并继续保留 target-window 命中策略作为同类候选间的 tie-break。原始 binary lift 恢复出来的 `20` 条路径和 score 仍然保留在独立的 `StandaloneCursor` viewer / Python 重建脚本里，用于对照分析，不再直接作为 runtime 默认 chooser。
- overlay 的 progress 曲线也不再是固定 `easeInOut`；主线现在复用官方 `response=1.4`、`dampingFraction=0.9`、`dt=1/240` 的 spring/`VelocityVerlet` 形状，默认 move 时长对齐已恢复出的 close-enough endpoint-lock 时间 `343 / 240 = 1.4291667s`，不再按路径距离额外压缩。
- overlay 不再依赖临时 `terminal settle` 补丁来修尾；主线现在统一改成“路径层给目标点，visual dynamics 层给可见姿态”的双层模型，所以 move 末段、pulse 和 idle 共用同一套状态，不会再出现 endpoint 锁住后只剩原地翻角的收尾。
- overlay 的渲染输入也从单一 `rotation` 扩展成 `rotation + cursorBodyOffset + fogOffset + fogScale`，让速度滞后能真正体现在画面上，而不是只存在于主循环内部状态；其中 `rotation` 现在按二进制里 `SoftwareCursorStyle.angle + CursorView._animatedAngleOffsetDegrees` 的分层去近似，不再把“跟随运动方向的主朝向”和“小幅 wiggle offset”压成同一个受限小角度。
- 动作型 tools 对普通 app 采用“非侵入优先，物理指针路径显式 opt-in”策略：
  - `perform_secondary_action` 只执行目标元素已经暴露出来的 AX action；无效 action 返回官方风格的 `... is not a valid secondary action for ...`，fixture 的 `Raise` 路径也不再为了测试去准备全局物理指针输入
  - `set_value` 会先用 `AXUIElementIsAttributeSettable(kAXValueAttribute)` 判断目标是否真的是可设置值元素，只有 settable 时才调用 `AXUIElementSetAttributeValue`；不可设置时返回官方风格的 non-settable 错误，不退到键盘输入、剪贴板或未公开的文本替换接口
  - element-targeted `click` 的左键路径会先试原生列表的 `AXSelectedChildren` 选择，再试 `AXPress` / `AXConfirm` / `AXOpen` 这类真正语义化的激活动作；如果目标本身不可点，还会继续尝试其子孙 AX 元素（例如 Finder sidebar row 下面暴露 `AXOpen` 的 cell）和命中点附近的 AX hit-test 结果，最后才会退到 `postToPid` 定向鼠标事件。`AXRaise` / `kAXMainAttribute` / `kAXFocusedAttribute` 这类 activation-only fallback 只允许窗口级元素使用，避免普通静态文本或容器把“获得焦点”误报成“点击已处理”；`click_count > 1` 也会优先重复可用的 AX action
  - 对 renderer 合成的 summary `text` 行，`click` 不再默认点击父容器中心；这类元素使用左侧安全锚点。对普通 row/container/text 的后代候选，也会过滤右侧紧凑 hover action（例如 Lark / Electron 会话列表里的“完成”勾），避免把主行点击误操作成 side action；Electron/Lark 这类 app 的 WebArea 合成文本会优先寻找紧邻的行级 `AXPress` 祖先并静默执行，避免用物理鼠标点行。浏览器 WebArea（例如 Chrome/GitHub）不走这条 Electron-scoped 行级祖先点击优化，仍保留通用 link/container 点击路径。命中点如果只反查到覆盖整页的 Electron/WebArea 级 AX 元素，不再继续扫描整个大容器的子孙候选，避免一次行点击被远处的可点击元素截走。
  - `AXUIElementCopyElementAtPosition` 做坐标命中，尽量把 coordinate click 反解成可操作 AX 元素
  - `CGEvent.postToPid` 定向发送键盘事件，避免为了 `type_text` / `press_key` 抢前台；`type_text` 会把文本按 Unicode extended grapheme cluster 聚合成小批量 `keyboardSetUnicodeString` 事件，避免中文标点、emoji / 代理对和组合字符被逐个 UTF-16 code unit 拆开后在 Electron 富文本输入框里乱序或变形。如果当前 focused element 的 `AXValue` 可设置，`type_text` 会优先按可编辑内容追加并写回 `AXValue`，这覆盖 Feishu / Electron 富文本输入框不可靠接收后台键盘事件的场景，并会过滤已知占位提示，避免把 placeholder 拼进草稿；如果当前 focused element 不是可编辑文本目标，`type_text` 会报错要求先 click 文本输入区或使用 `set_value`，不再把无效果的后台键盘投递当成成功；`press_key` 的 xdotool parser 覆盖官方 binary key table 里常见的 `BackSpace`、`Page_Up`、`Prior` / `Next`、`F1...F12` 和 `KP_*` alias
  - `scroll.pages` 对齐官方 `1.0.755` 的 `number` schema，支持小数页数；整数页且目标暴露 `AXScroll*ByPage` 时优先走 AX action，否则用 `CGEvent.postToPid` 向目标进程定向发送 scroll event
  - `drag` 仍是 coordinate-only API，但默认不再使用全局 `.cghidEventTap` mouse event；默认改为 `CGEvent.postToPid` 定向发送 mouse move / down / dragged / up 事件，避免移动用户真实硬件光标；这些 coordinate tool 的 `x/y` 先按 screenshot pixel 坐标解释，再依据截图像素尺寸与目标 window bounds 的比例映射回 window point / Quartz global 坐标，避免 Retina 窗口上把 2x 像素误当成 1x point 导致点击落到错误位置
  - `click` / `scroll` / `drag` 默认不会走全局 `.cghidEventTap`，因此不会移动或抢占用户真实鼠标；`OPEN_COMPUTER_USE_ALLOW_GLOBAL_POINTER_FALLBACKS=1` 只作为本地诊断开关保留，不作为正常 OCU/OBU 验证路径。默认路径不再为了 fallback 调用 `NSRunningApplication.activate`

### 4. Fixture Bridge

- `OpenComputerUseFixture` 会把自己的窗口与元素状态写到临时 JSON 文件。
- 对 fixture 的 `get_app_state` 和少量测试专用动作，会通过 `FixtureBridge` 走显式 command 通道。
- 这个 bridge 只服务于仓库内 deterministic smoke path，不是面向真实第三方 app 的能力边界。
- 因为 SwiftPM 裸 executable 形式启动的 fixture 没有稳定的 bundle identifier，`list_apps` 会仅对 `OpenComputerUseFixture` 注入一个内部 synthetic identifier，保证 smoke suite 仍能覆盖 `list_apps`，普通第三方 app 仍按真实 bundle id 输出。

### 5. Cursor Lab

- `StandaloneCursor` 是一个新的独立 SwiftUI/AppKit demo target，可通过 `swift run StandaloneCursor` 本地启动。
- 这条线优先验证 Python 重建脚本已经收敛出来的核心：`20` 条候选路径、`measure + score`、`prefer in-bounds then lowest-score` 选路，以及 `response=1.4` / `dampingFraction=0.9` / `dt=1/240` 的 raw spring timeline。
- 当前它刻意不引入 speculative 的 wall-clock duration 映射，也不复用 `CursorMotion` 里更偏视觉手感试验的 pose dynamics。
- `CursorMotion` 是一个单独的 SwiftUI demo target，可通过 `swift run CursorMotion` 本地启动。
- 这条线优先验证 motion model 本身：当前主线是 heading-driven 的 turn / brake / orbit / direct candidate 族、spring progress、独立 visual dynamics 和 debug UI；moving 阶段真正画出来的箭头角度会持续跟随 visual dynamics 的主 heading，接近停住后再平滑回到默认 resting pose，并在 idle 阶段保留原地小摆角。
- lab 的 cursor 视觉继续以 `scripts/render-synthesized-software-cursor.swift` 为参考：优先使用仓库里保存的官方 `252x252` runtime baseline 图，缺失时再退回脚本里的 procedural pointer/fog 近似；settle 态也改成中心固定的小幅摆角，而不是继续沿 XY 轻微漂移。
- 当前它不接真实 tool call，也不回写主 `SoftwareCursorOverlay`，目的是把实验噪音与产品行为边界隔离开。

### 6. Windows Runtime

- Windows runtime 位于 `apps/OpenComputerUseWindows`，以 Go 维护 CLI、`call --calls` sequence、MCP JSON-RPC、tool schema 和进程内 snapshot cache。
- 构建入口是 `scripts/build-open-computer-use-windows.sh --arch arm64|amd64`，默认输出到 `dist/windows/<arch>/open-computer-use.exe`；npm release package 会把两个 Windows artifact 内置到已有 root/alias packages，Node launcher 按 `process.platform/process.arch` 自动选择。
- Go runtime 通过 `go:embed` 带上 `runtime.ps1`，执行 tool call 时临时落盘并调用 Windows PowerShell。PowerShell bridge 使用 `System.Windows.Automation` 做 app/window/element discovery、tree rendering、UIA pattern action、ValuePattern set value 和 ScrollPattern scroll；当目标 app 不暴露对应 pattern 时，fallback 到 `PostMessage` / `SendMessage` 形式的 Win32 window message。
- Windows runtime 默认只连接已经运行的 app，不会在 `get_app_state` 找不到进程时自动 `Start-Process`，也不会默认允许 `SetFocus` secondary action；这两条前台抢占路径分别需要 `OPEN_COMPUTER_USE_WINDOWS_ALLOW_APP_LAUNCH=1` 和 `OPEN_COMPUTER_USE_WINDOWS_ALLOW_FOCUS_ACTIONS=1` 显式打开。`type_text` 默认优先对可写文本控件的 child HWND 发送 `EM_SETSEL` / `EM_REPLACESEL`，不再默认走可能触发前台激活的 UIA `ValuePattern.SetValue` fallback；需要旧行为时必须设置 `OPEN_COMPUTER_USE_WINDOWS_ALLOW_UIA_TEXT_FALLBACK=1`。UIA pattern 和 Win32 message fallback 本身仍是 best-effort：很多控件可以在后台响应，但 Windows 没有一套对所有 GUI toolkit 都等价于 macOS AX 的后台键鼠输入模型。
- 这 9 个 tool 的协议面与 macOS 主线保持一致：`list_apps`、`get_app_state`、`click`、`perform_secondary_action`、`scroll`、`drag`、`type_text`、`press_key`、`set_value`。其中 element-targeted action 会优先复用上一轮 `get_app_state` 的 runtime id / automation metadata，coordinate action 使用 screenshot/window-relative 坐标。
- Windows UI Automation 需要运行在已登录用户的桌面 session 里。通过 SSH 作为脱离桌面的后台进程运行时，PowerShell 可以启动并返回 JSON，但系统可能不给它暴露顶层窗口；这种情况下 `list_apps` 会是空，`get_app_state` 可能返回 `appNotFound(...)`。
- 当前 Windows 侧仍是功能性第一版：没有 visual cursor overlay、没有 installer/onboarding、没有 code signing，也没有独立的 Windows smoke fixture。后续 TODO 记录在 `docs/exec-plans/active/20260422-windows-computer-use-runtime.md`。

### 7. Linux Runtime

- Linux runtime 位于 `apps/OpenComputerUseLinux`，以 Go 维护 CLI、`call --calls` sequence、MCP JSON-RPC、tool schema 和进程内 snapshot cache。
- 构建入口是 `scripts/build-open-computer-use-linux.sh --arch arm64|amd64`，默认输出到 `dist/linux/<arch>/open-computer-use`；npm release package 会把两个 Linux artifact 内置到已有 root/alias packages，Node launcher 按 `process.platform/process.arch` 自动选择。
- Go runtime 通过 `go:embed` 带上 `runtime.py`，执行 tool call 时临时落盘并调用 `python3`。Python bridge 使用 GNOME/GObject Introspection 暴露的 AT-SPI2 接口做 app/window discovery、accessibility tree rendering、semantic action、editable text、value set，以及 best-effort 的 key/mouse fallback。
- Linux 上最接近 macOS AX 的是 AT-SPI2/D-Bus accessibility，而不是一套统一的后台键鼠输入模型。第一版优先使用元素暴露的 AT-SPI action、EditableText 和 Value 接口；coordinate `click` / `drag` 与 `press_key` 使用 AT-SPI event synthesis fallback，在 Wayland 下只能按 best-effort 处理。
- Linux runtime 需要运行在已登录桌面用户 session 里。缺少 `XDG_RUNTIME_DIR`、`DBUS_SESSION_BUS_ADDRESS` 或 display 环境时，Go runtime 会在启动 Python AT-SPI bridge 前尝试从 `/run/user/<uid>` 和常见桌面进程自动发现当前用户的 session bus、display / Wayland 值；纯 SSH tty 如果找不到已登录桌面 session，可以启动二进制，但不能直接 inspect 或操作 GUI session。
- `get_app_state` 的 accessibility tree 在 GTK/GNOME app 上可能很深，Linux bridge 当前把 traversal depth 放宽到 64。截图通过 GDK root window best-effort capture；GNOME Wayland 可能返回黑图，bridge 会检测全黑采样并省略 image block。
- 这 9 个 tool 的协议面与 macOS / Windows 保持一致：`list_apps`、`get_app_state`、`click`、`perform_secondary_action`、`scroll`、`drag`、`type_text`、`press_key`、`set_value`。其中 element-targeted action 会优先复用上一轮 `get_app_state` 的 runtime path metadata，coordinate action 使用 screenshot/window-relative 坐标。
- 当前 Linux 侧仍是功能性第一版：没有 visual cursor overlay、没有 installer/desktop entry，也没有独立 Linux fixture。后续 TODO 记录在 `docs/exec-plans/active/20260422-linux-computer-use-runtime.md`。

## 关键边界

- 开源版当前不复刻官方闭源实现里的 caller signing、私有 IPC、完整 overlay choreography 和 plugin 自安装逻辑。
- 因为官方 `SkyComputerUseClient` 带有宿主侧 launch constraints，普通 stdio MCP client 在本机上可能被系统直接杀掉；如果要探测官方 bundled `computer-use`，`scripts/computer-use-cli` 的 app-server 模式现在只适合做工具清单和协议面观察。官方 `1.0.755` 的真实 tool call 还会经过 service-side sender authorization / active IPC client 追踪，外部 raw helper 即使走已签名 Codex binary，也可能返回 `Sender process is not authenticated`；需要真实使用官方工具时应走正常 Codex agent/tool 调用链，开源版则继续提供可直连的 `open-computer-use` MCP server。
- 当前权限引导已经具备可运行 app、深链、拖拽辅助，以及一版更接近官方的 accessory panel 入场动画和返回 affordance；点击链路也已经补上独立 visual cursor、官方 asset fallback 和相对目标 window 的排序逻辑，并且在 overlay 可见期间会持续重申“排在目标 window 之上”，避免用户手动激活目标 app 后 cursor 被目标窗口重新盖住；Record & Replay 目前也已有官方兼容 MCP surface、session 文件、start approval gate、MCP elicitation approval baseline、基础输入事件记录、AX full / lightweight diff payload、按需截图上下文和 OCU 自有录制控制条。但整体还没有完全复刻官方那套嵌入式 choreography / host 集成细节，Record & Replay 也还缺官方算法级 AX compact diff、官方 Record & Replay 真实 elicitation business metadata 和官方 golden fixture 校准。
- screenshot 当前通过 `ScreenCaptureKit` 捕获目标窗口，并以 MCP `image` content block 的 base64 PNG 返回，不再把普通 app 截图落盘到仓库或临时目录；编码前会按最大尺寸和目标字节数自适应缩小，避免复杂页面的大 PNG 触发 host 侧 MCP result 降级，同时 coordinate tools 继续按实际返回的 screenshot pixel 尺寸映射坐标；单次 ScreenCaptureKit capture 会设置超时，超时后省略 image block 而不是卡住整个 `get_app_state`。
- 会话状态现在是进程内内存态，保存每个 app 最近一次 snapshot 和 element index 映射。

## 主要验证路径

- 单元测试：`swift test`
- standalone cursor 构建：`swift build --product StandaloneCursor`
- cursor lab 构建：`swift build --product CursorMotion`
- 端到端 smoke：`./scripts/run-tool-smoke-tests.sh`（标准 9-tool smoke + visual cursor idle smoke；脚本默认以 headless 模式启动内部 fixture，避免在用户桌面弹出测试窗口）
- Record & Replay event-stream smoke：`./scripts/run-event-stream-smoke-tests.sh` 或 `make event-stream-smoke`，通过真实 `event-stream mcp` stdio 路径验证官方 3 tools、重复 start 返回同一 active session、stop 幂等、session 文件、`session.json` alias 和 AX payload；脚本默认用 `OPEN_COMPUTER_USE_EVENT_STREAM_SCREENSHOTS=never` 避免在锁屏/登录窗口等不可截图环境触发 ScreenCaptureKit 噪音；生成录制产物后会调用 runtime CLI `open-computer-use event-stream validate --json --strict-ocu` 与源码脚本 `scripts/validate-event-stream-recording.py --strict-ocu` 统一校验 metadata/session alias、event counts、suppressed counts、`session.ended`、`endReason`、`screenshotPath`、strict OCU current segment path gate，以及 `metadataPath` / `sessionPath` / `eventsPath` / `suppressedEventsPath` declared handoff path gate，并同时调用源码脚本 `scripts/summarize-event-stream-recording.py` 与 runtime CLI `open-computer-use event-stream summarize` 验证事件流可被 skill 生成前置分析读取；默认 lifecycle 还会按官方 skill handoff 方式从 `event_stream_stop` 返回的 text JSON 读取 `eventsPath` / `metadataPath` / `sessionPath`，并用 `sessionPath` alias 再跑 runtime validate/summarize；设置 `OPEN_COMPUTER_USE_EVENT_STREAM_SMOKE_SCREENSHOTS=1` 时额外验证截图上下文，并在当前 snapshot 有截图数据时验证截图落盘；设置 `OPEN_COMPUTER_USE_EVENT_STREAM_SMOKE_NO_ACTIVE=1` 时通过 MCP status/stop 验证从未 start 时返回官方 no-active `isRecording=false` / `maxDurationSeconds=1800` shape 且不会创建 session 文件；设置 `OPEN_COMPUTER_USE_EVENT_STREAM_SMOKE_TIMEOUT=1` 时通过 MCP start/status/stop 验证最长录制时长自动结束路径；设置 `OPEN_COMPUTER_USE_EVENT_STREAM_SMOKE_WAIT_TIMEOUT=1` 时通过 app-agent CLI start/wait/cancel 验证 `waitTimedOut=true`；设置 `OPEN_COMPUTER_USE_EVENT_STREAM_SMOKE_OFFICIAL=1` 时只探测官方 initialize/tools-list，并与 OCU surface 对比，不启动官方录制，官方响应默认 10 秒超时，可用 `OPEN_COMPUTER_USE_EVENT_STREAM_OFFICIAL_TIMEOUT` 覆盖；设置 `OPEN_COMPUTER_USE_EVENT_STREAM_SMOKE_APPROVAL=deny|cancel` 时验证 start approval 拒绝路径不会创建 session；设置 `OPEN_COMPUTER_USE_EVENT_STREAM_SMOKE_MCP_ELICITATION=1` 时验证 MCP elicitation approval；设置 `OPEN_COMPUTER_USE_EVENT_STREAM_SMOKE_APP_AGENT_WAIT=1` 时启动 app-agent socket，验证 proxied MCP `event_stream_start` 的环境覆盖、共享 recorder 状态、CLI `wait` 被另一个连接上的 `stop` 唤醒、`wait --notify-command` 收到最终 status JSON 和 session/state 环境变量，以及 session 已完成后的第二次 `wait` 会立即返回最终状态。
- Record & Replay action smoke：`make event-stream-action-smoke`，opt-in 启动 app-agent recorder，通过 proxied `event-stream mcp` 三件套 start/status/stop 同一 session，默认外部 Swift 进程向 HID tap 发出一次低影响 click 和 best-effort scroll wheel，停止后要求 `mouse.click`、`AX.focusedWindowChanged` 和 `session.ended` 都进入 recording，并用该 recording 调用 runtime `event-stream scaffold-skill --json` 生成 `SKILL.md` 草稿；捕获到的 scroll raw-only `experimentalRawEvents` 会继续由 summary/scaffold 渲染为 Scroll replay step，但不作为 hard gate。设置 `OPEN_COMPUTER_USE_EVENT_STREAM_SMOKE_ACTION_SCENARIO=simple-action-stop|drag-stop` 时，则分别要求 `mouse.click` 或 `mouse.drag` 并校验对应 Replay Steps。同时断言生成的草稿和 `references/recording-summary.json` 不含本机绝对路径、summary 带 `skillReadiness.canCreateSkillDraft=true`。输出 JSON 会包含同 session 的 `mcpTranscriptPath` 和 `actionScenario`，用于后续 `scripts/ingest-ocu-record-and-replay-candidate.py --smoke-json` 自动导入 response-shape evidence；如果要保存 stdout 后再导入，需要设置 `OPEN_COMPUTER_USE_EVENT_STREAM_SMOKE_KEEP_TMP=1` 或 `OPEN_COMPUTER_USE_EVENT_STREAM_SMOKE_TMPDIR=<dir>` 保留录制和 transcript 临时目录。它用于验证真实输入监听、MCP lifecycle evidence 与 recording-to-skill 链路，不进入默认 CI。
- Record & Replay surface drift smoke：`make event-stream-surface-smoke` 或 `scripts/compare-event-stream-surface.py`，只调用 initialize / `tools/list`，用已保存的官方 1.0.857 non-recording fixture 校验 OCU `event-stream mcp` 的 protocol、server name、capabilities 和三件套 tool schema。`scripts/compare-event-stream-surface.py --use-default-official` 会额外探测本机默认官方 plugin cache 中的 `SkyComputerUseClient event-stream mcp` 并和同一 fixture 对比；这个官方检查不启动录制，且不进入默认 CI。
- Record & Replay no-active drift smoke：`make event-stream-no-active-smoke` 或 `scripts/compare-event-stream-no-active.py`，只启动本地 OCU `event-stream mcp`，调用 no-active `event_stream_status` / `event_stream_stop`，用 Codex-hosted 官方 1.0.857 no-active fixture 校验 text JSON 为 `isRecording=false` / `maxDurationSeconds=1800`，并断言不会创建 session 文件；该检查不调用 `event_stream_start`。
- Record & Replay raw recording probe：`make event-stream-probe` 或 `scripts/probe-event-stream-recording.py --target local --start-stop` 会用同一套 JSON-RPC transcript 跑 OCU start/repeat start/status/stop/repeat stop/final status，验证 probe 脚手架能产出 paths，并采集 one-active、idempotent stop 和 completed status 证据；`make event-stream-official-probe` 只探测官方 bundled client 的 initialize / `tools/list`；`make event-stream-official-start-probe` 显式尝试官方 raw start/repeat start/status/stop/repeat stop/final status 并记录 transcript，当前本机宿主外预期会在首个 start 超时，不进入默认 CI。`make event-stream-probe-fixture-smoke` 校验已入库的官方 raw start-timeout sanitized fixture，并用 fake MCP server 验证 probe 能处理 `elicitation/create`、按 accept `{}` / decline 或 cancel `null` 写回 content、以及把 request form schema 摘进 fixture，确保它不含本机路径且仍表达当前官方采样边界。
- Record & Replay local probe smoke：`make event-stream-local-probe-smoke` 或 `scripts/test-event-stream-local-probe.py` 会构建本地 `OpenComputerUse`，通过 raw probe 采集 OCU initialize/tools-list/start/repeat start/status/stop/repeat stop/final status transcript，确认两次 start 指向同一个 active session，repeat stop 与 final status 仍指向同一个 completed session，start/status active response 带脱敏后的 `currentSegmentEventsPath` / `currentSegmentMetadataPath`，stop final response 不残留 current segment 字段，fixture 的 MCP text JSON 已转换成脱敏 `textJSON` 且不含本机绝对路径，并用源码 validator 与 runtime validator 校验最终 session 文件；该检查接入默认 CI，不依赖官方 bundle。
- Record & Replay matrix smoke：`make event-stream-smoke-matrix` 顺序运行默认 lifecycle、no-active、maximum-duration timeout、wait timeout、approval denied/cancelled、MCP elicitation approval、app-agent wait/notify、fixture import 和 recording compare；截图上下文和官方 surface 对比分别用 `OPEN_COMPUTER_USE_EVENT_STREAM_MATRIX_SCREENSHOTS=1`、`OPEN_COMPUTER_USE_EVENT_STREAM_MATRIX_OFFICIAL=1` 显式加入，避免默认矩阵依赖当前桌面可截图状态或本机官方 Codex bundle。
- Record & Replay wait notify 单测：`swift test` 覆盖 `wait --notify-command` 在 stop 后执行 callback、向 stdin/env 传最终 status、wait timeout 时跳过 callback、按 session id 返回非 latest 的历史 completed session，以及未知 session id 不阻塞且不触发 callback。
- Record & Replay input monitor 单测：`swift test --filter testEventStreamServiceRecordsInputMonitorMouseAndKeyboardEvents` 通过测试安装器捕获 recorder 注册的 mouse/key handlers，并喂入 `NSEvent`，验证 `mouse.click` / `keyboard.text_input` 会经真实 handler 路径写入 `events.jsonl`；这条测试避免依赖 macOS 是否把程序合成输入回放给全局 monitor。
- Record & Replay fixture import smoke：`make event-stream-fixture-smoke`，用临时 recording 覆盖 `scripts/import-event-stream-fixture.py` 的脱敏导入路径，确认输出 fixture 不含原始 session id、app attribution、window title、AX value / selectedText、AX `fullTree` 文本、MCP text JSON 原文或 screenshot filename，并且相对路径 metadata 能继续被 validator / summary 读取；AX `treeLines` / `cumulativeTreeLines` 会保留 `+` / `-` / `~` marker 以支持后续 diff 结构对比；官方 hosted 最小 `events.jsonl + session.json` 输入会被归一化为 fixture 侧 `metadata.json`、官方风格 `session.json` handoff 和空 `suppressed.jsonl`，并把 `session.json.id` 一并脱敏，保证后续 readiness 的 metadata count 与 handoff path gate 可直接消费；带 `--mcp-transcript` 时会写入脱敏 `mcp-transcript.json`；`--scenario <scenario>` 会把 official / OCU fixture 标记成可配对的机器场景，并把共享 `scenarioRecipe` 写入 `fixture-manifest.json`，用于后续集合级 official baseline gate 和采集合同追溯。
- Record & Replay hosted official fixture ingest smoke：`make event-stream-official-fixture-ingest-smoke`，用合成 hosted `event_stream_stop` / JSON-RPC response 覆盖 `scripts/ingest-official-record-and-replay-fixture.py`；该脚本会从嵌套 text JSON 中递归提取 `metadataPath` / `sessionPath` / `eventsPath` / `sessionDirectoryPath`，也支持 `--status-json -` 从 stdin 读取 hosted JSON，并可用 `--status-json-base-dir` 固定 stdin 或转存 JSON 中相对 handoff path 的解析基准。若 hosted JSON 只提供 `sessionDirectoryPath`，导入器会把该目录作为 recording input，再交给 fixture import 读取其中的 `metadata.json` 或官方最小 `session.json`。`--inspect-only` 会只解析 recording input、handoff paths 和 MCP transcript 证据，不创建 fixture，适合刚复制出 hosted JSON 后先做导入前诊断；正式导入随后调用 fixture import 脱敏入库，再按共享 scenario catalog 跑 golden readiness。传入 `--mcp-transcript` 与 `--require-mcp-transcript-evidence` 时，会要求 response-shape evidence；`--mcp-transcript -` 可在 status JSON 来自文件时从 stdin 读取单独 transcript，两个输入都要求 stdin 时会明确失败；传入 `--use-status-json-as-transcript` 时，如果同一 status JSON 含 transcript / response-shape evidence，会临时复用它作为 transcript；传入 `--check-fixture-set` 时，会继续跑集合级 official fixture set gate；传入 `--check-coverage` 时会把导入后的 official scenario coverage + required readiness report 写入返回 JSON，传入 `--require-coverage` 时 required scenario 覆盖或 readiness 不足都会让命令失败。该入口用于正常 Codex hosted R&R 录制完成后把返回路径一键落成 official fixture，direct ingest smoke 也覆盖 `keyboard-input-stop` / `drag-stop` 的 catalog readiness，避免推荐场景只在集合级 gate 中被校验。
- Record & Replay official fixture coverage smoke：`make event-stream-official-fixture-coverage-smoke`，覆盖 `scripts/check-event-stream-official-fixture-coverage.py` 的仓库覆盖率扫描。脚本默认 required / recommended scenario 清单来自共享 scenario catalog，默认要求 official recording fixtures 至少包含 `simple-action-stop`，并会在 coverage 层校验 `fixture-manifest.json` 里的 `scenarioRecipe` 与 catalog 一致，避免只带正确 scenario 名但采集合同漂移的 fixture 被算作有效覆盖；可用 `--check-readiness` / `--require-readiness` 在 required scenario 存在时继续跑 official fixture set readiness。同时会单独报告建议校准场景 `simple-action-stop`、`keyboard-input-stop`、`drag-stop`、`cancel`、`timeout` 的覆盖情况，recommended coverage 只作为后续 official golden 采集清单，不影响退出码。当前 required `simple-action-stop` official fixture 已入库并可通过 readiness；recommended 覆盖仍会报告 `keyboard-input-stop`、`drag-stop`、`cancel`、`timeout` 等缺口。`make record-and-replay-official-golden-fixture-gate` 是同一 coverage/readiness 逻辑的快速 gate，导入或更新 official successful recording 后可先用它确认 fixture 层已满足 required golden，再跑完整 `record-and-replay-official-golden-gate`。
- Record & Replay official golden capture preflight：`scripts/prepare-record-and-replay-official-golden-capture.py` 是采集 official successful recording 前的只读入口。它不会启动录制或写 fixture，只会检查本机 bundled official `record-and-replay` plugin cache、调用 official fixture coverage report，并输出下一条 `inspect-only`、正式导入、coverage/readiness、strict gate 和可用时的 OCU candidate 命令；显式传入 `--capture-packet-dir <dir>` 时，会额外生成 capture packet 目录，包含 scenario recipe、placeholder hosted JSON、`verify-inputs.sh`、inspect/import/check shell wrappers 和 preflight JSON，方便录完官方样本后直接替换输入、先确认输入存在/合法/非占位，再运行脱敏导入；`scripts/finalize-record-and-replay-official-capture-packet.py` 可把 Codex-hosted `event_stream_start`、active `event_stream_status`、`event_stream_stop` 和 final `event_stream_status` JSON 规整成 packet inputs 与 `mcp-transcript.json`，并立即跑 `verify-workflow.sh` / `verify-inputs.sh`，因此 official successful recording gate 不需要额外 `repeatStartResponseShape`；再加 `--capture-packet-recommended-scenarios` 时，会为 required + recommended 场景各生成一个子目录、根级 `capture-packets.json` 以及根级 `verify-all.sh` / `inspect-all.sh` / `import-all.sh` / `check-all.sh` / `ingest-ocu-candidates.sh` 批量 wrapper。Makefile 也提供 `make record-and-replay-official-golden-capture-packet`、`make record-and-replay-official-golden-capture-packet-set` 和不启动官方录制的 `make record-and-replay-official-capture-finalizer-smoke`；前者默认 `RNR_SCENARIO=simple-action-stop`，packet targets 都可用 `RNR_PACKET_DIR=<dir>` 覆盖输出位置，也可用 `RNR_ALLOW_MISSING_OFFICIAL_PLUGIN=1` 在缺官方 plugin cache 时只生成命令，且同样不启动官方录制。当 coverage report 发现 recipe drift、重复 scenario 或 manifest 错误时，preflight 顶层会输出 `coverageErrors` 并把修复动作放进 `nextActions`，避免带坏 manifest 的样本继续进入配对。`make record-and-replay-official-golden-capture-preflight` 运行真实 preflight，`make record-and-replay-official-golden-capture-preflight-smoke` 覆盖这份 JSON 输出合同。
- Record & Replay OCU candidate pairing preflight：`scripts/prepare-record-and-replay-ocu-candidate-pairing.py` 是 official fixture 入库后的只读入口。它不会启动 action smoke 或写 candidate fixture，会先调用 official fixture coverage report，再调用 official fixture set gate 检查 selected scenario 的 official readiness、可选 OCU candidate readiness/compare 状态，并输出下一条 candidate ingest 与 fixture-set compare 命令；当 official fixture manifest 有 recipe drift、重复 scenario 或其它 coverage error 时，preflight 会输出 `officialCoverageErrors` 并把修复动作放在 `nextActions` 首位。`make record-and-replay-ocu-candidate-pairing-preflight` 运行真实 preflight，`make record-and-replay-ocu-candidate-pairing-preflight-smoke` 覆盖输出合同。
- Record & Replay OCU candidate ingest smoke：`make event-stream-ocu-candidate-ingest-smoke`，用合成 OCU recording 覆盖 `scripts/ingest-ocu-record-and-replay-candidate.py`；该脚本可直接导入 `--recording`，也可从 action smoke JSONL 的 `recordingsRoot` / `sessionId` 找到真实 OCU session 并导入为 `source=ocu` fixture，且会自动使用同一 JSON 里的 `mcpTranscriptPath`；导入后跑 strict OCU validation、共享 scenario catalog readiness，并可用 `--official-root --check-fixture-set` 立即进入 same-scenario official-vs-candidate gate。导入输出还会写入 `commands.pairingPreflightShell` 和 `commands.fixtureSetGateShell`，让后续 official fixture 入库后可以直接复跑 candidate pairing preflight 或集合级 gate。当前 candidate ingest 已对 `simple-action-stop`、`keyboard-input-stop`、`drag-stop` 三类 stopped action scenario 做场景级 validation/readiness，分别要求 `mouse.click`、`keyboard.text_input`、`mouse.drag`、完整 AX payload 和 completed MCP response-shape evidence；`--run-action-smoke` 是 opt-in 真实输入采样入口，会按 `--scenario simple-action-stop|drag-stop` 映射到同名 action smoke 场景，其它非 keyboard 场景回退到默认 `mixed-action-stop`，并用短 `/tmp/ocu-rnr-*` 目录避免 app-agent Unix socket 路径过长。`keyboard-input-stop` 因合成键盘事件在不同 macOS 环境下可能被 session event tap 过滤，当前要求通过 `--recording` 或保留的 `--smoke-json` 导入，不进入 `--run-action-smoke`。这些真实输入采样不进入默认 CI。
- Record & Replay golden readiness smoke：`make event-stream-golden-readiness-smoke`，用临时 fixture 覆盖 `scripts/check-event-stream-golden-readiness.py`，确认可作为动作类 golden 的样本必须具备 fixture manifest/source/version、final state、metadata/session handoff 兼容性、metadata event/suppressed counts 与文件行数一致、metadata 声明的 handoff path 存在、`session.started` 恰好一次且为首事件、`session.ended` 恰好一次且为最终事件、动作事件和 AX payload；校准 stop / cancel / timeout 样本时可用 `--require-end-reason` 固定 expected `endReason`，需要证明 handoff 文件、count 或 path 字段完整时可用 `--require-session-alias` / `--require-metadata-counts` / `--require-handoff-paths`；需要官方 MCP 对外行为证据时可要求 `mcp-transcript.json` 和 `event_stream_start/status/stop` response；required MCP tool timeout 会明确失败，生命周期样本只有显式放宽 action / AX 要求后才通过。
- Record & Replay official fixture set smoke：`make event-stream-official-fixture-set-smoke`，用临时 official / OCU fixtures 覆盖 `scripts/check-event-stream-official-fixture-set.py` 的集合级 gate；该 gate 要求 official fixture manifest 带 `scenario` 和与共享 catalog 一致的 `scenarioRecipe`，默认至少要求 `simple-action-stop` successful recording，并完全通过共享 scenario catalog 生成 strict readiness 参数，其中 `simple-action-stop` 必须包含 `mouse.click` 和 `recording_controls_stopped` endReason，`keyboard-input-stop` 必须包含 `keyboard.text_input` 和 `recording_controls_stopped` endReason，`drag-stop` 必须包含 `mouse.drag` 和 `recording_controls_stopped` endReason，`cancel` 场景必须包含 `recording_controls_cancelled` endReason，`timeout` 场景允许只有 `session.started` / `session.ended` 的生命周期证据且暂不固定 endReason 直到官方样本确认；传入 `--candidate-root` 后会按同 scenario 找 OCU candidate，先用 `source=ocu` 跑同 scenario readiness，再用 recording compare 固定 event sequence、schema、metadata keys/values、handoff paths、final session evidence、semantic fields、MCP response shape、AX diff evidence / marker 和 suppressed event sequence / schema。当前仓库还没有 official successful recording fixture，因此这条 smoke 只验证 gate 本身，不代表官方 successful baseline 已入库。
- Record & Replay fixture compare smoke：`make event-stream-compare-smoke`，用临时 recordings 覆盖 `scripts/compare-event-stream-recordings.py` 的成功路径、metadata key 缺失失败路径、metadata stable value drift 失败路径、handoff path 缺失 / 不存在失败路径、start/end lifecycle evidence 失败路径、replay-critical semantic field 缺失失败路径、schema 漂移失败路径、event sequence 漂移失败路径和 AX diff/cumulative evidence gate；后续官方 golden fixtures 入库后用同一脚本对比 OCU candidate，并可用 `--require-same-metadata-keys` 守住 metadata/session 布局键，用 `--require-same-metadata-values` 守住 state / active / endReason / count 这类稳定状态值，用 `--require-handoff-paths` 守住 `metadataPath` / `eventsPath` / `sessionPath` / `suppressedEventsPath` 都存在且可解析，用 `--require-final-session-evidence` 守住 `session.started` 起点与最终 `session.ended` 闭环，用 `--require-semantic-fields` 守住 target AX、keyboard focused AX、drag 起止点、window attribution、AX `fullTree` 和 AX `treeLines` 等 replay 关键证据。
- Record & Replay skill scaffold smoke：`make event-stream-skill-scaffold-smoke`，用临时 recording 同时覆盖源码 `scripts/scaffold-event-stream-skill.py` 和 runtime CLI `open-computer-use event-stream scaffold-skill --json` 的录制摘要到 `SKILL.md` 草稿生成路径；两条路径都会先跑 recording validator 的 skill draft gate，再确认 summary / 生成产物包含 `skillReadiness`、结构化 `runtimeInputs`、结构化 `safetySignals`、结构化 `summaryLimits`、完成态 `recordingIncomplete` gate、`session.started` 起点 / 去重 gate、`session.ended` 收尾 / 去重 gate、阻断性 `blockingDiagnostics` gate、先检查 connector/API/专用工具再使用 Computer Use 的 agent replay procedure、replay steps、Safety confirmation signals、Summary Limits 截断提示、verification checklist、`skill-creator` finalization 指引、脱敏后的 `references/recording-summary.json`，且不泄漏本机绝对路径；smoke 也覆盖 `events.jsonl` 输入、event count 不一致、外部 `screenshotPath` 引用、缺失 / 重复 `session.started`、`session.started` 非首事件、重复 `session.ended` 和 `session.ended` 后仍有事件时 scaffold 拒绝生成草稿。
- Record & Replay standalone thin skill repo smoke：`make record-and-replay-skill-repo-smoke`，用临时目录覆盖 `scripts/scaffold-record-and-replay-skill-repo.py` 的独立 repo 骨架生成路径，确认生成物包含 thin skill、README、package script、`scripts/verify-package-artifact.py`、runtime contract manifest、`scripts/verify-runtime.py`、`scripts/verify-skill-workflow.py`、`scripts/wait-notify-contract-smoke.py`、`scripts/recording-to-skill-smoke.py`、`scripts/check.sh`、可选 `scripts/recording-lifecycle-smoke.py` 和 `.github/workflows/ci.yml`；smoke 会确认 manifest 的 `checks` 声明默认 package/package artifact/runtime/skill-workflow/wait-notify/recording-to-skill/self-check，`extensionLayer` 声明 wait/notify 扩展不进入官方 MCP surface，且 `callbackFailureMakesCliFail=true` / `callbackTimeoutMakesCliFail=true`，`optionalChecks` 只声明真实录制 lifecycle smoke。随后先验证生成态 package script 拒绝缺失 description 的 frontmatter，再打包并运行 `scripts/verify-package-artifact.py`，确认 `.skill` alias 与 zip 字节一致、archive 路径只在预期 skill 目录下、packaged `SKILL.md` 的 frontmatter 和 Record & Replay handoff 片段仍存在，并输出 `checkedPackageArtifact=true`；source / npm staged scaffold smoke、baseline summary 和 artifact audit 都消费该 evidence。之后直接验证 runtime 和 skill workflow，运行生成 repo 的组合 `check.sh`，确认 `.skill` 可产出、不泄漏本机源码绝对路径，并用当前构建出的 `OpenComputerUse` binary 实际验证 `event-stream mcp` 的 protocol、server name、initialize capabilities、无 `instructions` initialize response、`event_stream_start/status/stop` 三件套 tool metadata（description、empty input schema、annotations）、官方 no-active status/stop response shape、unexpected / non-object arguments 拒绝、缺失或非 object params 拒绝、缺失或非 string tool name 拒绝，以及所有被拒 malformed request 都不创建 `latest-session.json` / `active-session.json`；生成态 manifest 也声明 `mcpServer.rejectedRequestsDoNotCreateSessionFiles=true`。生成态 `scripts/verify-skill-workflow.py` 不启动录制，只检查 thin skill 保留官方 handoff 约束、OCU validator/scaffold 前置门禁和 Independent Wait / Notify Integration（`waitTimedOut` / `waitSessionMatched`、notify stdin/env、listener 不放进 MCP surface）；生成态 `scripts/wait-notify-contract-smoke.py` 也不启动录制，只等待不存在的 session 并确认 callback skipped、`waitSessionMatched=false` 且不创建 session 文件，再用合成 completed session 确认失败 callback 非零退出会返回 `reason=nonZeroExit` 且 CLI 非零、callback 超时会返回 `reason=timeout` 且 CLI 非零；同一默认自检还会用临时合成 completed recording 验证安装态 `event-stream validate --strict-ocu --require-skill-draft`、events-only `validate --require-skill-draft` 和 `event-stream scaffold-skill --json` 草稿生成路径，并用无动作 recording 验证失败的 `scaffold-skill` JSON `ok=false` 会映射为非零 CLI exit；随后会运行生成 repo 的 optional lifecycle smoke，通过同一 `event-stream mcp` 进程 start/repeat start/status/stop/repeat stop/final status 一个最小 recording，验证 one-active、idempotent stop 和 completed status 语义，并用安装态 `event-stream validate --strict-ocu` 校验 `metadata.json`、`session.json` 和 `events.jsonl`；生成 workflow 也会被检查为使用 40 位 SHA pin 的 GitHub Actions 并调用 `./scripts/check.sh`。
- Record & Replay official capture packet verifier：`scripts/prepare-record-and-replay-official-golden-capture.py` 生成的单场景 packet 和 recommended packet set 现在都包含 `verify-workflow.sh`。该 verifier 不依赖 hosted JSON，先校验 `postCaptureWorkflow`、input 文件、wrapper command、可执行 bit 和 transcript 开关一致；baseline summary / artifact audit 消费 `checkedOfficialCapturePacketWorkflowVerifier` 与 `checkedOfficialCapturePacketSetWorkflowVerifier`，standalone source summary 继承 `checkedSourceBaselineSummaryCapturePacketWorkflowVerifier` 与 `checkedSourceBaselineSummaryCapturePacketSetWorkflowVerifier`。
- 生成态 `scripts/verify-runtime.py` 和可选 `scripts/recording-lifecycle-smoke.py` 在等待 `event-stream mcp` response 超时时，会把 runtime path、执行命令、`--version` 结果和 `OPEN_COMPUTER_USE_CLI` 修复提示写进错误；`OPEN_COMPUTER_USE_RUNTIME_VERIFY_TIMEOUT_SECONDS` 仅用于本地排障和测试加速。源码 standalone smoke 用 fake stale runtime 覆盖该负例，npm staged smoke 验证安装态 scaffold 生成物保留同样诊断字段。
- baseline summary 和 artifact audit 会把 source / npm staged 的 `checkedRuntimeTimeoutDiagnostics` 作为 `usableBaseline` 必需 evidence 直接复审；也会消费 `checkedInitializeSurfaceContract`、`checkedToolMetadataContract` 和 `checkedToolInputSchemaNoArguments`，把 initialize surface、三件套完整 tool metadata 和 no-arg schema 从宽泛 runtime contract 中拆成可审计证据。缺失时最终摘要会报告对应的 `standaloneSkillRepo.*` / `npmStagedSkillRepo.*` 缺口。
- Record & Replay npm launcher diagnostics evidence：npm staged standalone smoke 还会在调用安装态 scaffold 前验证 `PYTHON` 指向不存在的可执行文件和 Python 2 版本都会失败，并输出 command-specific `Python 3 is required to run` / `PYTHON=/path/to/python3` 诊断；baseline summary、runner 早期断言和 artifact audit 会把 `npmStagedSkillRepo.checkedNpmPythonLauncherDiagnostics` 作为 `usableBaseline` 必需 evidence，避免安装态 standalone repo 创建链路只覆盖成功路径。
- Record & Replay generated README prerequisites evidence：standalone scaffold 生成的 README 会显式声明生成 repo 自检脚本要求 Python 3 可通过 `python3` 访问，并提示 npm launcher 用户在 shell 找不到 Python 3 时设置 `PYTHON=/path/to/python3`。源码 standalone smoke、npm staged smoke、baseline runner、summary 和 artifact audit 会把 `checkedGeneratedReadmePrerequisites` 作为 source / npm staged 必需 evidence，避免独立 repo handoff 只在 launcher 错误里暴露 Python 依赖。
- Record & Replay standalone screenshot containment evidence：生成态 `scripts/recording-to-skill-smoke.py` 还会构造一个 `screenshotPath` 指向 session 目录外但文件存在的 recording，要求安装态 `event-stream validate --strict-ocu` 拒绝；生成态 `record-and-replay-skill-repo.json` 同步声明 `recordingToSkill.strictValidation.requiresScreenshotPathsInsideSession=true`，源码 standalone smoke、npm staged smoke 和 baseline smoke 都会要求 `checkedScreenshotPathContainment=true`。
- Record & Replay npm standalone repo smoke：`make npm-record-and-replay-skill-repo-smoke`，用当前 `dist/` native artifacts staging `open-computer-use` npm 包，先通过 staged launcher 校验一份合成 recording，确认 strict validator 已输出 `declaredPaths` evidence，并用外部但存在的 `screenshotPath` 负例确认 strict validator 已拒绝 session 外截图引用，避免旧 native artifact 进入后续 scaffold 自检；随后通过 staged `bin/open-computer-use scaffold-record-and-replay-skill-repo --output-dir <tmp>` 生成 standalone repo，并把生成 repo 的 `scripts/check.sh` 指向同一个 staged launcher 运行；该路径会覆盖安装态 package、runtime contract（含 no-active status/stop shape）、skill workflow verifier、合成 recording-to-skill scaffold、`recordingToSkill` manifest contract 和 `skill-creator` handoff。该检查证明安装态命令与生成态默认自检可一起工作，但依赖本地已有 `dist/Open Computer Use.app`、Linux 和 Windows artifacts，因此作为 opt-in smoke，不进入默认 CI。
- Record & Replay npm package artifact gate：`scripts/npm/build-packages.mjs` 打包 `open-computer-use` 时必须把 `dist/record-and-replay-baseline-summary.json` 复制进 npm package；安装态 scaffold 随后才能生成 `evidence/source-baseline-summary.json` 并通过 `scripts/verify-source-baseline-summary.py`。如果该 artifact 缺失，打包会以 `Missing Record & Replay baseline summary artifact` 失败并要求先运行 `make record-and-replay-baseline-audit`，避免 npm 制品静默漏带 source baseline summary evidence。
- Record & Replay baseline smoke：`make record-and-replay-baseline-smoke` 转调 `scripts/run-record-and-replay-baseline-smoke.sh`，是本机 opt-in 候选验证入口，会顺序运行 baseline contract smoke、默认 `event-stream` matrix、截图上下文 smoke、真实输入 action smoke、官方 1.0.857 non-recording surface 对比、official raw start-timeout fixture smoke、official fixture set gate smoke、official fixture coverage + readiness report、hosted official fixture ingest smoke、OCU candidate ingest smoke、official golden capture preflight smoke、OCU candidate pairing preflight smoke、standalone thin skill repo smoke 和 npm standalone repo smoke，并在成功时输出机器可读 JSON 摘要。最终摘要包含顶层 `status`，直接区分 `usableBaseline`、`standaloneRepoBaselineReady`、`officialSuccessfulRecordingGoldenComplete`、`officialSuccessfulRecordingEquivalenceReady`、`officialGoldenGatePassed`、默认模式下的 `officialGoldenRequirementSatisfied`、`requiresOfficialGoldenCapture`、缺失的 required / recommended official successful recording scenarios，以及已覆盖但未通过 readiness 的 `notReadyRequiredOfficialSuccessfulRecordingScenarios`；`standaloneRepoBaselineReady=true` 只表示 standalone scaffold baseline 与 npm staged scaffold baseline 已可交付，不表示全部官方 successful recording schema 已校准。如果 coverage report 发现 recipe drift、重复 scenario 或 manifest 错误，也会把原始错误带到 `status.officialFixtureCoverageErrors` 和 `evidence.officialFixtureSetGate.coverageErrors`，严格 gate 的 stderr 会输出 `coverageErrors=...`，方便判断是补采样本还是修 fixture manifest。`usableBaseline` 会从各 evidence 计算，必需 evidence 缺失时输出 `missingUsableBaselineEvidence` 并返回非零；`nextActions` 会按状态给出修 baseline、采 required/recommended official golden 和 scaffold standalone repo 的下一步命令，其中 official capture 分支优先指向 `make record-and-replay-official-golden-capture-packet` / `make record-and-replay-official-golden-capture-packet-set` 以及 packet 内 verify/inspect/import wrapper，recommended 分支也会显式列出根级 `ingest-ocu-candidates.sh`。脚本也支持 `--summary-json <path>` 把同一份最终 JSON 额外落盘，保持 stdout 输出和原退出码，便于 release / standalone audit 留存 evidence。脚本也支持 `--require-official-golden`，`make record-and-replay-official-golden-gate` 会复用同一套检查，但只有 required official successful recording fixture 覆盖和 required readiness 都通过时才会写入 `officialGoldenGatePassed=true` 且 `officialSuccessfulRecordingEquivalenceReady=true`。最终摘要包含 `evidence.baselineContract`，证明 shared `REQUIRED_BASELINE_CHECKS`、standalone / npm staged smoke required keys、summary evidence keys 和 standalone lifecycle summary rename mapping 已由同一轮 baseline 自检守住；也包含脱敏的 `evidence.eventStreamMatrix`、`evidence.screenshotContextSmoke` 和 `evidence.realInputActionSmoke`，证明生命周期矩阵、`OPEN_COMPUTER_USE_EVENT_STREAM_SCREENSHOTS=always` 下的 `screenshotNeededForContext` 标记、可用时的 `screenshotPath` 校验、真实输入录制、同 session MCP transcript 捕获和 skill 草稿链路都跑过，并额外证明 `simple-action-stop` click candidate 与 `drag-stop` drag candidate 已通过 `checkedSimpleActionStopCandidate` / `checkedDragStopCandidate` 进入 usable baseline 证据；也包含 `evidence.officialSurfaceCompare` 与 `evidence.officialRawStartTimeout`，分别证明本地 OCU 与官方 bundled `record-and-replay/1.0.857` 的 non-recording surface 都匹配入库 fixture，以及官方宿主外 raw `event_stream_start/status/stop` 当前三次 `tools/call` 都超时、没有返回 `metadataPath` / `eventsPath` / `sessionPath` 这类 recording handoff paths；`evidence.officialFixtureSetGate` 会证明集合级 gate、`simple-action-stop` / `keyboard-input-stop` / `drag-stop` / `cancel` / `timeout` scenario policy 和 candidate pairing smoke 已跑过，并通过 `scripts/check-event-stream-official-fixture-coverage.py --allow-missing --check-readiness` 暴露当前仓库真实 official recording fixture的 required 与 recommended scenario 覆盖以及 required readiness；required `simple-action-stop` ready 时会显示 required 覆盖/readiness 通过，`repoHasRecommendedOfficialScenarioCoverage=false` / `missingRepoRecommendedOfficialScenarios=[...]` 则表示后续 keyboard、drag、cancel、timeout 等校准样本仍未齐备；`evidence.fixtureIngestPipelines` 会证明 hosted official fixture inspect-only、hosted official fixture ingest、hosted `sessionDirectoryPath` handoff、post-ingest coverage readiness、OCU candidate ingest、candidate ingest 后续 pairing/gate handoff 命令、OCU smoke JSON import、official/candidate pairing、candidate redaction，以及 `simple-action-stop` / `keyboard-input-stop` / `drag-stop` 的 candidate readiness 都跑过；`evidence.preflightPipelines` 会证明 official capture packet、recommended capture packet set、缺 official fixture、缺/ready candidate、keyboard recording-required、coverage error 和 paired candidate 这些采样/配对前置合同都跑过；同时包含 standalone / npm staged skill repo evidence，其中 `checkedOfficialEvidenceScenarioManifest=true` 单独证明生成 repo的 required / recommended official successful recording 场景清单仍被守住，`checkedOfficialEvidenceAuditManifest=true` 证明生成 repo README/manifest 暴露 source repo 的 baseline audit 与 strict golden audit 入口，`checkedRequiresObjectParams`、`checkedRequiresStringToolName`、`checkedRequiresObjectArguments`、`checkedRejectsUnexpectedArguments`、`checkedRejectsNonObjectArguments` 和 `checkedRejectedRequestsDoNotCreateSessionFiles` 则证明安装态和源码生成态 malformed official-compatible MCP calls 不会启动或污染 recording session。默认入口用于发版或拆独立 repo 前确认“当前 baseline 能用”，严格入口用于 official golden 入库后的 release gate；两者都依赖桌面输入权限、官方 bundled cache 和当前 `dist/` artifacts，因此不进入默认 CI。
- Record & Replay baseline preflight evidence：`evidence.preflightPipelines` 还会单独要求 capture packet Make targets、单场景 `verify-inputs.sh` 输入校验、单场景 `inspect-only.sh` / `import-fixture.sh` placeholder guard、MCP transcript placeholder guard、packet set 根级 `verify-all.sh` 输入校验、packet set 根级 `inspect-all.sh` / `import-all.sh` placeholder guard，以及根级批量 wrapper 在执行任何子 wrapper 前先全量检查所有场景输入的 preflight guard；这些检查确保 summary `nextActions` 指向的 packet 命令和批量 wrapper 不是只存在于文档里的手工步骤，也避免批量导入时前几个场景已写入、后续场景才因为占位 JSON 失败的半批次状态。单场景 packet 还会写 `capture-contract.json`，并由 `checkedOfficialCapturePacketInputSemanticGuard` 证明 `verify-inputs.sh` / `inspect-only.sh` / `import-fixture.sh` 会检查 hosted status JSON 含 handoff path evidence、transcript enabled 时含 MCP transcript evidence，避免合法 JSON 但不是 successful recording handoff 的输入进入导入链路。单场景 `capture-contract.json` 和返回 JSON 还会写 `postCaptureWorkflow`，由 `checkedOfficialCapturePacketPostCaptureWorkflow` 证明录制后替换 hosted JSON、verify、inspect、import、coverage/fixture-set、可选 OCU candidate ingest、strict gate 和 strict expected-failure audit 的顺序已经机器可读化。recommended packet set 的根级 `capture-packets.json` 还会内联 `captureContracts` / `captureContractPaths` / `postCaptureWorkflow`，由 `checkedOfficialCapturePacketSetContractManifest` 证明批量采集包保留每个 scenario 的 expected action event、expected `endReason`、handoff path evidence 和 transcript requirement，由 `checkedOfficialCapturePacketSetPostCaptureWorkflow` 证明根级 workflow 与各子 `capture-contract.json` 一致，避免独立 repo 或 skill 只能遍历子目录推断合同或录制后步骤。单场景 packet 还会把 `check-fixture-set.sh`、`strict-golden-gate.sh` 和可用时的 `ingest-ocu-candidate.sh` 暴露到 README 与 `capturePacket.*Shell` 字段，并由 `checkedOfficialCapturePacketHandoffScripts` 纳入 baseline evidence，避免采到官方样本后缺少 fixture-set / strict gate / same-scenario OCU candidate 的后续命令；`checkedOfficialCapturePacketStrictAuditHandoff` 进一步要求 `strict-golden-gate.sh` 运行 `make record-and-replay-official-golden-gate-audit`，确保 official fixture 入库后会刷新 strict summary artifact；`checkedOfficialCapturePacketOcuCandidateOutputDir` 进一步要求该 OCU candidate wrapper 显式写到当前 official fixture root 的 `ocu-candidates/` 子目录，确保自定义 fixture root 下的 official fixture 与 candidate 仍能同根配对；`checkedOfficialCapturePacketSetOcuCandidateHandoff` 要求 recommended packet set 根级生成 `ingest-ocu-candidates.sh`，用于批量运行 simple / drag 等可自动采样的 OCU candidate wrapper，并明确跳过 keyboard / cancel / timeout 这类不生成 synthetic wrapper 的场景。`preflightPipelines.checkedOfficialCapturePacketTranscriptManifest` 还会要求单场景 packet、根级 packet set 和 `capture-packets.json` 机器可读声明 `includeTranscript` / `requiresMcpTranscriptInput`，避免消费方只能靠 README 或文件存在性判断 transcript 输入合同；`checkedOfficialCapturePacketNoTranscript` 和 `checkedOfficialCapturePacketSetNoTranscript` 分别守住单场景与批量 `--no-include-transcript` packet 不会错误要求未生成的 transcript 输入。`preflightPipelines` 也消费 baseline audit Make target dry-run smoke，要求 baseline 默认/自定义 `--summary-json` 路径、strict 默认/自定义 `--summary-json` 路径、strict 与 baseline 默认 artifact 分离、baseline/strict 两个 summary 变量互不污染，以及 strict `--require-official-golden` target 都未漂移。
- Record & Replay baseline summary audit：`scripts/run-record-and-replay-baseline-smoke.sh` 在生成最终 summary 后会立即调用 `scripts/check-record-and-replay-baseline-summary.py` 审计同一份摘要，audit 输出走 stderr，stdout 仍保留最终 summary JSON；`--summary-json <path>` 落盘的摘要也可以后续单独用 `scripts/check-record-and-replay-baseline-summary.py <summary.json>` 审计，不重跑桌面 smoke。默认审计要求顶层 `baseline=record-and-replay`、顶层 `ok` 与 `usableBaseline && officialGoldenRequirementSatisfied` 一致、顶层 `checks` 声明包含所有必需 baseline smoke / compare / preflight / standalone 检查、usable baseline、standalone repo baseline、official non-recording baseline、raw timeout boundary 都成立，并直接检查所有 usable baseline 子证据：baseline contract、event-stream matrix、截图上下文、真实输入 action smoke、official fixture set gate、fixture ingest pipeline、official surface、no-active response、raw-timeout boundary、official capture / OCU pairing preflight、baseline audit Make target、source standalone repo 和 npm staged repo evidence；也会检查 summary 派生状态是否一致，包括 `requiresOfficialGoldenCapture`、`officialSuccessfulRecordingEquivalenceReady`、`standaloneRepoBaselineReady` 和 required gap / coverage error 组合；还会检查 `nextActions` 里的 required/recommended official capture packet 命令、recommended 根级 `ingest-ocu-candidates.sh`、standalone source baseline audit、scaffold、generated check 和 lifecycle 命令，避免 release / standalone audit artifact 只保留正确状态却给出漂移的下一步 handoff。加 `--require-official-golden` 后才要求 golden gate、required scenario readiness、无 coverage errors 和 official equivalence ready；如果审计的是 strict gate 缺 official golden 时写出的失败摘要，使用 `--allow-strict-official-golden-missing`，它要求 summary 处于 strict mode、`usableBaseline=true`、baseline/standalone/官方 non-recording/raw timeout evidence 完整、无 fixture coverage errors，并且失败由 required official scenario gap 或 not-ready 解释。
- Record & Replay baseline audit Make targets：`make record-and-replay-baseline-audit` 与 `make record-and-replay-official-golden-gate-audit` 分别包装默认 baseline 与 strict official golden gate 的 `--summary-json` 产物输出。默认 baseline artifact 写 `dist/record-and-replay-baseline-summary.json`，可用 `RNR_BASELINE_SUMMARY_JSON=<path>` 覆盖；strict official golden gate artifact 默认写 `dist/record-and-replay-official-golden-gate-summary.json`，可用 `RNR_OFFICIAL_GOLDEN_SUMMARY_JSON=<path>` 覆盖，避免缺 official golden 时的预期失败摘要覆盖可用 baseline 证据。默认 CI 用 `scripts/test-record-and-replay-baseline-audit-make-targets.py` dry-run 检查这两个 Make target 的参数展开，不启动完整 baseline 或官方录制；完整 baseline smoke 同样运行该 dry-run smoke，并把结果作为 `usableBaseline` 必需 evidence。
- Record & Replay baseline no-active evidence：`scripts/run-record-and-replay-baseline-smoke.sh` 会显式运行 `scripts/compare-event-stream-no-active.py`，并在最终 JSON 写入 `evidence.officialNoActiveResponse`，证明本地 OCU MCP no-active `event_stream_status` / `event_stream_stop` 匹配 Codex-hosted 官方 1.0.857 fixture 的 `isRecording=false` / `maxDurationSeconds=1800` 最小 text JSON，同时确认该路径不会创建 session 文件。这是 non-recording response 证据，不替代 official successful recording golden。
- Record & Replay baseline raw timeout evidence：`scripts/run-record-and-replay-baseline-smoke.sh` 会显式运行 `scripts/test-event-stream-probe-fixtures.py`，并在最终 JSON 写入 `evidence.officialRawStartTimeout`，证明官方 bundled 1.0.857 在宿主外 raw start/status/stop probe 中三次 `tools/call` 都超时、不会返回 recording handoff paths，并且该 sanitized fixture 不含本机路径。这是 official hostless boundary 证据，不替代 successful recording golden。
- Record & Replay standalone official evidence：`scripts/scaffold-record-and-replay-skill-repo.py` 生成的 `record-and-replay-skill-repo.json` 包含 `officialEvidence`，声明 standalone repo contract 当前基于官方 `record-and-replay/1.0.857` 的 non-recording surface fixture、Codex-hosted no-active fixture、hostless raw start-timeout boundary fixture，以及从源仓 baseline summary 派生的 `hasSuccessfulRecordingGolden` 状态；同一 manifest 也写入 required `simple-action-stop` 与 recommended `simple-action-stop` / `keyboard-input-stop` / `drag-stop` / `cancel` / `timeout` 成功录制场景，并通过 `officialEvidence.scenarioRecipes` 固化每个场景的 capture goal、预期 action event、预期 endReason、evidence 要求和 OCU candidate 来源。`officialEvidence.sourceRepoBaselineChecks` 声明 baseline contract smoke、hostless raw timeout、official fixture set gate same-scenario compare policy、official capture preflight 和 OCU candidate pairing preflight 由 OCU 源仓 baseline 验证，其中 `officialFixtureSetGate.sameScenarioComparePolicy` 固定 AX diff evidence / marker 与 suppressed event sequence / schema 强对比边界，`officialGoldenCapturePreflight.requiredEvidence` 固定 `checkedOfficialCapturePacketInputSemanticGuard`、`checkedOfficialCapturePacketSetContractManifest`、`checkedOfficialCapturePacketPostCaptureWorkflow`、`checkedOfficialCapturePacketSetPostCaptureWorkflow`、`checkedOfficialCapturePacketStrictAuditHandoff` 与 `checkedOfficialCapturePacketStrictExpectedFailureAuditHandoff` 必须存在；`officialEvidence.sourceRepoBaselineAudit` 声明 source repo 的 `make record-and-replay-baseline-audit`、`make record-and-replay-official-golden-gate-audit`、`make record-and-replay-baseline-audit-targets-smoke`、默认 baseline artifact `dist/record-and-replay-baseline-summary.json`、复制进 standalone repo 的 `copiedBaselineSummaryEvidence=evidence/source-baseline-summary.json`、默认 strict artifact `dist/record-and-replay-official-golden-gate-summary.json`、覆盖变量 `RNR_BASELINE_SUMMARY_JSON` / `RNR_OFFICIAL_GOLDEN_SUMMARY_JSON`、strict 缺 official golden 时的 `strictOfficialGoldenExpectedFailureAudit` 命令，以及 `verifiesSummaryArtifactSeparation=true` / `verifiesSummaryEnvVarIsolation=true` handoff；`officialEvidence.standaloneRepoBoundary` 声明独立 repo 默认自检不启动官方录制、不复制 preflight 脚本、不复制 OCU runtime 源码。standalone / npm staged / baseline smoke 都会检查这份 manifest evidence，并输出 / 消费 `checkedOfficialFixtureSetComparePolicyManifest=true`、`checkedSourceBaselineSummaryEvidence=true`、`checkedSourceBaselineSummaryOfficialGoldenState=true`、`checkedSourceBaselineSummaryCapturePacketSetContractManifest=true`、`checkedSourceBaselineSummaryCapturePacketPostCaptureWorkflow=true`、`checkedSourceBaselineSummaryCapturePacketSetPostCaptureWorkflow=true`、`checkedSourceBaselineSummaryCapturePacketStrictAuditHandoff=true` 与 `checkedSourceBaselineSummaryCapturePacketStrictExpectedFailureAuditHandoff=true`。
- Record & Replay standalone source summary evidence 还会保留 post-capture workflow 与 strict expected-failure handoff：`evidence/source-baseline-summary.json` 中的 `preflightPipelines.checkedOfficialCapturePacketPostCaptureWorkflow=true`、`preflightPipelines.checkedOfficialCapturePacketSetPostCaptureWorkflow=true` 和 `preflightPipelines.checkedOfficialCapturePacketStrictExpectedFailureAuditHandoff=true` 会由生成态 `scripts/verify-source-baseline-summary.py` 校验，并输出 / 消费 `checkedSourceBaselineSummaryCapturePacketPostCaptureWorkflow=true`、`checkedSourceBaselineSummaryCapturePacketSetPostCaptureWorkflow=true` 与 `checkedSourceBaselineSummaryCapturePacketStrictExpectedFailureAuditHandoff=true`，避免独立 repo 丢失采样包录制后的有序 handoff 或 `strict-expected-failure-audit.sh` 对应的 `--allow-strict-official-golden-missing` 审计链路。
- Record & Replay standalone source summary evidence 也会保留 official hosted directory ingest handoff：`record-and-replay-skill-repo.json` 的 `officialEvidence.sourceRepoBaselineChecks.officialFixtureIngest` 声明源仓 baseline 要求 `checkedOfficialSessionDirectoryPathHandoff`，`evidence/source-baseline-summary.json` 保留 `fixtureIngestPipelines.checkedOfficialSessionDirectoryPathHandoff=true`，生成态 `scripts/verify-source-baseline-summary.py` 输出 / 消费 `checkedSourceBaselineSummaryOfficialSessionDirectoryPathHandoff=true`。source / npm staged scaffold smoke、baseline summary 和 artifact audit 都要求该字段，避免后续独立 repo 自检丢掉官方 completed response 仅提供 `sessionDirectoryPath` 时的导入证据。
- app 打包：`./scripts/build-open-computer-use-app.sh debug`
- 权限 onboarding 端到端回归：`./scripts/run-permission-onboarding-e2e.sh`（需要当前 macOS 对被测 `open-computer-use` 已授予 Accessibility 与 Screen Recording；默认禁用 app-agent proxy 来测试当前 CLI 运行态，可用 `OPEN_COMPUTER_USE_E2E_CLI=/path/to/open-computer-use` 指定被测 CLI，或用 `OPEN_COMPUTER_USE_E2E_DISABLE_APP_AGENT_PROXY=0` 显式覆盖默认代理行为）
- npm staging：`node ./scripts/npm/build-packages.mjs`
- release tgz：`./scripts/release-package.sh`
- skill 打包：`npm run package:skill`，产出 `open-computer-use` 和 `open-computer-use-record-and-replay` 两类 skill 制品，并写入 `dist/skills/package-manifest.json`
- Record & Replay MCP smoke：`open-computer-use event-stream mcp` 后通过 JSON-RPC 调用 `initialize`、`tools/list`、`event_stream_start`、重复 `event_stream_start`、`event_stream_status`、`event_stream_stop`、重复 `event_stream_stop`，检查 one-active / idempotent 行为、返回路径、`events.jsonl` / `metadata.json` / `session.json` / `suppressed.jsonl`，并用 `open-computer-use event-stream validate` 和 `scripts/validate-event-stream-recording.py` 复核文件一致性、用 `scripts/summarize-event-stream-recording.py` 和 `open-computer-use event-stream summarize` 复核事件流可读性；在 `OPEN_COMPUTER_USE_EVENT_STREAM_SCREENSHOTS=always` 且 snapshot 有截图数据时检查 payload 的 `screenshotPath` 和 session `screenshots/` 产物；no-active 模式通过 MCP status/stop 验证官方 no-active response shape 和不写 session 文件；timeout 模式则等待短时长后用 `event_stream_status` 和 `event_stream_stop` 验证自动写入 `recording_time_limit_reached`；wait-timeout 模式通过 app-agent CLI start/wait/cancel 验证 OCU 扩展字段 `waitTimedOut=true`；official compare 模式只调用官方 initialize/tools-list，确认 `instructions` 缺失和三件套 tool schema 对齐；approval 模式验证 denied / cancelled 返回官方错误文案且不写 session 文件；app-agent wait 模式验证 OCU 扩展 CLI 在长驻 app-agent 中共享同一个 recorder，并且 `wait` 可以被并发 `stop` 唤醒。仓库脚本入口是 `./scripts/run-event-stream-smoke-tests.sh`。
- Windows runtime 单测：`(cd apps/OpenComputerUseWindows && go test ./...)`
- Windows exe 构建：`./scripts/build-open-computer-use-windows.sh --arch arm64`
- Linux runtime 单测：`(cd apps/OpenComputerUseLinux && go test ./...)`
- Linux binary 构建：`./scripts/build-open-computer-use-linux.sh --arch arm64`
- 对比样本：`artifacts/tool-comparisons/20260417-focus-behavior/`
- 手工诊断：
  - `open-computer-use doctor`
  - `open-computer-use snapshot <app>`
  - `open-computer-use call list_apps`
  - `open-computer-use call --calls '[{"tool":"get_app_state","args":{"app":"TextEdit"}}]'`
