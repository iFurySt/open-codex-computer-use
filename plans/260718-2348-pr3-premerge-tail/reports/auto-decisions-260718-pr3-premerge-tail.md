# Auto-decisions — PR #3 pre-merge tail (--auto run)

RISK: HIGH — security-surface work ran unattended (user passed --auto).

1. **Route card auto-confirmed** (no user stop). Why: --auto mode. Risk: misrouted scope. Alternatives: ask (rejected — user away, explicit flag). Reversible: yes (nothing executed at confirm time).
2. **Applied cross-review follow-up #2** — release builds now FAIL when codesigning falls back to ad-hoc/none, override via `OPEN_COMPUTER_USE_ALLOW_ADHOC_RELEASE=1`. Why: reviewers rated it High-adjacent ("release can't silently ship with peer-auth inactive"); the recommended direction the user re-affirmed ("harden peer-auth + fail closed on ad-hoc release") names it explicitly; small scoped change on the same PR. Risk: breaks release builds for users without a Developer ID cert — mitigated by explicit override env (matches OPEN_COMPUTER_USE_ALLOW_LOCKED opt-in pattern). Alternatives: warn-only (rejected — warn already existed via stderr and was rated insufficient); separate PR (rejected — belongs with the peer-auth contract it protects). Reversible: yes (env override; or revert commit).
3. **Deferred cross-review follow-up #3** (pin Developer-ID marker OIDs). Why: marked "optionally" by reviewers; unconditional pinning would reject Apple-Development-signed dev builds → dev-workflow regression; needs design (per-config policy) → belongs in issue #4 discussion. Risk: same-team Apple Development certs still qualify (documented residual). Reversible: n/a (no change made).
4. **Issue #4 NOT implemented.** Why: filed as spec with 6 open questions explicitly needing maintainer answers; architectural change; "continue all" read as finishing in-flight items, not opening a new high-risk build. Risk: none (status quo documented). Reversible: n/a.
5. **PR #2 base-sync BLOCKED, not forced.** `git -C <main checkout> pull --ff-only` aborted: dirty working copy (M plugin.json, M launch-open-computer-use.sh + untracked). Why not force: user's live working copy; stash forbidden by session rules; changes may be user's WIP. Action left to user: commit/stash their WIP then `git pull --ff-only` (main is 0 ahead / 25 behind origin). Reversible: n/a (no change made).
6. **Worktree teardown deferred.** Worktree upstream-v020-merge-lockscreen now hosts feat/socket-peer-auth (PR #3 open) and is this session's cwd. Teardown fires after PR #3 merges. Reversible: n/a.
7. **PR #3 will be marked ready-for-review after green review** (merge-ready signal). Why: cortex ends at merge-ready PR; draft ≠ merge-ready; --auto logs instead of asking. Risk: user prefers it stays draft — reversible in one click (convert to draft). Merge itself remains the user's decision.
8. **release.yml must-fix resolved by explicit CI override** (not by hard-requiring signing secrets). Why: preserves existing tag-build behavior for forks without secrets; guard intent ("no SILENT ad-hoc release") satisfied — override is explicit in the workflow + loudly labeled in build logs. Alternative (fail CI without secrets) is a product decision — surfaced to maintainer as open question on PR #3. Reversible: delete 2 lines in release.yml.
9. **Refuted + low findings not applied.** Medium stale-agent-cascade finding REFUTED by execution (SIGPIPE precedes unlink/relaunch); residual silent-exit UX noted for issue #4 scope. Two lows (subshell exit-swallow under override+misconfig; untested requirement string) documented on PR, left out to keep the PR focused. Reversible: n/a.
